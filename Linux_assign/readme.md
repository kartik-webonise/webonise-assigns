The file 'bashfile.sh' contains the commands uname,hostname,pwd,who,groups and ls.
The result of all these commands are written to the file 'assignment.log'

The file 'copy4.sh' is the bashfile which copies first four files from the current directory to the folder public_html

The file 'set777.sh' is the bashfile which finds out the file readme.txt and sets its permissions to 777.

The file 'assignment.log' contains the output of commands uname, hostname,pwd,who,groups and ls.

The file 'linux cmd.txt' contains all the command and bashfiles to the related assignment as per the index.
